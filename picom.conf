# Basic configuration
backend = "glx";
vsync = true;
paint-on-overlay = true;
glx-swap-method = 2;

# Opacity rules
opacity-rule = [ "0:_NET_WM_STATE@:32a *= '_NET_WM_STATE_HIDDEN'" ];

# Fading effects
fading = true;
fade-in-step = 0.03;
fade-out-step = 0.03;
no-fading-openclose = true;


